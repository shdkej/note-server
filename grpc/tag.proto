syntax = "proto3";
package grpcserver;

service TagManager {
    rpc GetTag(Message) returns (Note) {}
    rpc GetTags(Message) returns (stream Note) {}
}

message Note {
    string tag = 1;
}

message Message {
    string body = 1;
}
